{
  "name": "@oresoftware/ores-gh-bots",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Event-driven, dual-provider GitHub pull-request review gates for the ORES fleet, plus the nightly cross-org PR reconciler (dependency-graph-driven PR refresh, readiness-gated auto-merge) and fleet git hygiene.",
  "engines": {
    "node": ">=22.6.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "node apps/orchestrator/src/main.mjs",
    "worker": "node apps/orchestrator/src/main.mjs --worker-only",
    "cli": "node apps/cli/src/main.mjs",
    "hardening": "node apps/cli/src/hardening.mjs",
    "review": "node apps/runner/src/main.mjs review",
    "reconcile": "node --experimental-strip-types src/cli.ts",
    "hygiene": "node --experimental-strip-types src/hygiene-cli.ts",
    "build:flags2env": "node node_modules/node-gyp/bin/node-gyp.js rebuild --directory=node_modules/@oresoftware/f2e/clients/nodejs",
    "test": "npm run test:review && npm run test:nightly",
    "test:review": "node --test --test-reporter=spec",
    "test:nightly": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "npx --yes -p typescript@5 -p @types/node@22 tsc -p tsconfig.json --noEmit",
    "lint": "node scripts/lint.mjs",
    "lint:ores": "if [ -f .ores-lint/lint.sh ]; then sh .ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "check": "npm run lint && npm test",
    "verify:repository": "node scripts/verify-repository.mjs",
    "verify:github-apps": "node scripts/verify-github-apps.mjs",
    "verify:fleet-hardening": "node scripts/verify-fleet-hardening.mjs",
    "verify": "npm run verify:repository && npm run verify:github-apps && npm run verify:fleet-hardening",
    "ci:classify": "node scripts/classify-workflow-evidence.mjs"
  },
  "comments": {
    "typecheck": "The nightly reconciler and its tests use only Node built-ins; `npm run typecheck` pulls typescript/@types/node through npx on demand rather than adding them to the runtime dependency graph.",
    "build:flags2env": "CI installs with --ignore-scripts, then explicitly compiles only the lockfile-pinned flags2env native addon instead of enabling arbitrary dependency lifecycle scripts."
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  },
  "license": "MIT",
  "dependencies": {
    "@oresoftware/f2e": "https://codeload.github.com/ORESoftware/flags-2-env/tar.gz/5a44d6a38f06c2765e80676618fd72bf4e59b984"
  }
}
